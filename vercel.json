{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "build": {
    "env": {
      "VITE_BACKEND_URL": "$VITE_BACKEND_URL",
      "VITE_IDLE_TIMEOUT": "$VITE_IDLE_TIMEOUT"
    }
  }
}
